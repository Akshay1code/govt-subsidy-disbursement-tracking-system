Disbursement Plan Configuration module

This module provides endpoints to configure disbursement plan stages and release milestones.

Endpoints:
- POST /api/v1/disbursement/plan/{planId}/configure
- POST /api/v1/disbursement/release/{milestoneId}

Use H2 in-memory database for testing.